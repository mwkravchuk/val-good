

const RoleVisual = ({ favoriteAgent }) => {

  return (
    <div>
      <div>
        <span>favorite agent: {favoriteAgent ? favoriteAgent.agent : ""}</span>
      </div>
      <div>

      </div>
    </div>
  );
};

export default RoleVisual;